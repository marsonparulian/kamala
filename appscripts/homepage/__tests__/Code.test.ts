/**
 * This file only an example of test fo the `homepage` appscript project
 */

describe("Example of test suite for homepage project", () => {
  it("Should be true equal to true", () => {
    expect(true).toBe(true);
  });
});
